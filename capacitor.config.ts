import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MLMTeamStack',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
